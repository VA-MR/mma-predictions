# 🚀 Быстрый старт - Админ-панель MMA Project

## ✅ Проверено и работает!

API endpoints протестированы и возвращают данные из парсера:
- ✅ 1,081 бойцов
- ✅ 55 событий  
- ✅ 518 боев
- ✅ 12 организаций

---

## 📋 Шаг 1: Запуск Backend

Откройте терминал и выполните:

```bash
cd "/Users/v/Desktop/MMA project"

# Остановить старые процессы (если есть)
lsof -ti:8000 | xargs kill -9 2>/dev/null

# Запустить backend
python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
```

**Проверка:** Откройте http://localhost:8000/docs - должна открыться документация API

---

## 📋 Шаг 2: Запуск Frontend

Откройте **НОВЫЙ** терминал и выполните:

```bash
cd "/Users/v/Desktop/MMA project/frontend"

# Установить зависимости (если еще не установлены)
npm install

# Запустить dev сервер
npm run dev
```

**Проверка:** Должно появиться сообщение `Local: http://localhost:5173/`

---

## 📋 Шаг 3: Открыть админ-панель

Откройте браузер и перейдите:

### 🎯 http://localhost:5173/admin

Вы увидите админ-панель с 4 разделами:
- 🏢 **Организации** - 12 организаций
- 👤 **Бойцы** - 1,081 боец
- 📅 **События** - 55 событий
- 🥊 **Бои** - 518 боев

---

## 🧪 Быстрая проверка API

Выполните в терминале:

```bash
cd "/Users/v/Desktop/MMA project"
python3 test_admin_api.py
```

Должен вывести:
```
============================================================
TESTING ADMIN API ENDPOINTS
============================================================

1. Testing /api/admin/organizations...
   Status: 200
   Organizations found: 12

2. Testing /api/admin/fighters...
   Status: 200
   Fighters found: 5

3. Testing /api/admin/events...
   Status: 200
   Events found: 5

4. Testing /api/admin/fights...
   Status: 200
   Fights found: 5

============================================================
ALL TESTS COMPLETED
============================================================
```

---

## 🔧 Решение проблем

### ❌ Backend не запускается

```bash
# Проверить, что порт свободен
lsof -i:8000

# Если порт занят, освободить его
lsof -ti:8000 | xargs kill -9

# Запустить заново
cd "/Users/v/Desktop/MMA project"
python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
```

### ❌ Frontend показывает "Нет данных"

1. **Проверьте, что backend запущен:**
   ```bash
   curl http://localhost:8000/health
   ```
   Должен вернуть: `{"status":"healthy"}`

2. **Проверьте данные напрямую:**
   ```bash
   curl http://localhost:8000/api/admin/fighters?limit=3
   ```

3. **Откройте консоль браузера (F12):**
   - Перейдите на вкладку "Network"
   - Обновите страницу
   - Проверьте запросы к `/api/admin/*`
   - Если видите ошибки 404 или CORS - backend не запущен

### ❌ Данные не обновляются

Данные берутся из БД `mma_data.db`. Чтобы обновить данные:

```bash
cd "/Users/v/Desktop/MMA project"

# Запустить парсер
python3 main.py

# Показать статистику
python3 main.py --stats
```

---

## 📊 Проверка данных в БД

```bash
cd "/Users/v/Desktop/MMA project"
python3 -c "
from database.db import Database
from database.models import Fighter, Event, Fight
from sqlalchemy import func

db = Database('mma_data.db')
session = db.get_session()

print('Fighters:', session.query(func.count(Fighter.id)).scalar())
print('Events:', session.query(func.count(Event.id)).scalar())
print('Fights:', session.query(func.count(Fight.id)).scalar())
"
```

---

## 🎯 Полезные ссылки

После запуска:

- **Админ-панель:** http://localhost:5173/admin
- **Главная страница:** http://localhost:5173/
- **API документация:** http://localhost:8000/docs
- **API OpenAPI:** http://localhost:8000/openapi.json
- **Health check:** http://localhost:8000/health

---

## 📁 Структура проекта

```
MMA project/
├── mma_data.db          # База данных (1081 бойцов, 55 событий, 518 боев)
├── api/                 # Backend API (FastAPI)
│   ├── main.py         # Точка входа
│   └── routes/
│       └── admin.py    # Admin endpoints
├── frontend/           # Frontend (React + Vite)
│   └── src/
│       ├── pages/      # Страницы админки
│       └── components/ # Компоненты
└── database/           # Модели БД
    └── models.py
```

---

## 💡 Дополнительная информация

- Подробная документация: `ADMIN_README.md`
- Инструкции запуска: `START_ADMIN.md`
- Тестовый скрипт: `test_admin_api.py`

---

## ✨ Готово!

Админ-панель полностью настроена и готова к использованию. Все данные из парсера доступны для просмотра и редактирования.

