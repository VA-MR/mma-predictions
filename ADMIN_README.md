# Админ-панель MMA Project

## Описание

Административная панель для управления данными о бойцах, событиях, боях и организациях MMA.

## Реализованные возможности

### 🏢 Организации
- Просмотр списка всех организаций
- Автоматическое извлечение из базы событий
- Отображение количества событий по организациям

### 👤 Бойцы (Fighters)
- Просмотр списка всех бойцов из базы данных
- Поиск бойцов по имени
- Создание новых бойцов
- Редактирование информации о бойцах:
  - Имя (русское и английское)
  - Страна
  - Рекорд (победы, поражения, ничьи)
  - Физические параметры (возраст, рост, вес, размах рук)
  - Стиль боя, весовая категория, рейтинг
  - Детализация побед и поражений по методам
  - URL профиля
- Удаление бойцов

### 📅 События (Events)
- Просмотр списка всех событий
- Создание новых событий
- Редактирование информации о событиях:
  - Название
  - Организация
  - Дата и время (МСК)
  - Локация
  - URL и slug
  - Статус (предстоящее/прошедшее)
- Удаление событий

### 🥊 Бои (Fights)
- Просмотр списка всех боев
- Создание новых боев
- Редактирование информации о боях:
  - Выбор события
  - Выбор бойцов (Fighter 1 и Fighter 2)
  - Тип карты (основная/прелиминары)
  - Весовая категория
  - Количество раундов
  - Порядок боя
  - Время начала
- Удаление боев

## Технические детали

### Backend (API)
Создан новый роутер `/api/admin` с endpoints:

**Organizations:**
- `GET /api/admin/organizations` - Список организаций

**Fighters:**
- `GET /api/admin/fighters` - Список бойцов (с поиском)
- `GET /api/admin/fighters/{id}` - Получить бойца
- `POST /api/admin/fighters` - Создать бойца
- `PUT /api/admin/fighters/{id}` - Обновить бойца
- `DELETE /api/admin/fighters/{id}` - Удалить бойца

**Events:**
- `GET /api/admin/events` - Список событий
- `GET /api/admin/events/{id}` - Получить событие
- `POST /api/admin/events` - Создать событие
- `PUT /api/admin/events/{id}` - Обновить событие
- `DELETE /api/admin/events/{id}` - Удалить событие

**Fights:**
- `GET /api/admin/fights` - Список боев
- `GET /api/admin/fights/{id}` - Получить бой
- `POST /api/admin/fights` - Создать бой
- `PUT /api/admin/fights/{id}` - Обновить бой
- `DELETE /api/admin/fights/{id}` - Удалить бой

### Frontend

**Новые компоненты:**
- `AdminLayout` - Layout с боковой навигацией
- `AdminTable` - Универсальная таблица данных
- `AdminModal` - Модальное окно для форм

**Новые страницы:**
- `AdminDashboardPage` - Главная страница админки
- `AdminOrganizationsPage` - Управление организациями
- `AdminEventsPage` - Управление событиями
- `AdminFightsPage` - Управление боями
- `AdminFightersPage` - Управление бойцами

**Роутинг:**
- `/admin` - Дашборд
- `/admin/organizations` - Организации
- `/admin/events` - События
- `/admin/fights` - Бои
- `/admin/fighters` - Бойцы

## Доступ

В данный момент админ-панель доступна всем пользователям без авторизации. 
Для добавления ролей и ограничения доступа необходимо:

1. Добавить поле `role` в модель `User`
2. Создать middleware для проверки прав доступа
3. Добавить декоратор для защиты admin endpoints
4. На фронтенде добавить проверку роли перед отображением ссылки на админку

## Использование

1. Запустите backend: 
   ```bash
   cd "/Users/v/Desktop/MMA project"
   python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
   ```

2. Запустите frontend: 
   ```bash
   cd frontend
   npm run dev
   ```

3. Откройте браузер и перейдите на страницу `/admin`
4. Используйте навигацию слева для переключения между разделами

### Проверка API

Вы можете проверить работу API endpoints напрямую:

```bash
# Список бойцов
curl http://localhost:8000/api/admin/fighters?limit=5

# Список событий
curl http://localhost:8000/api/admin/events?limit=5

# Список боев
curl http://localhost:8000/api/admin/fights?limit=5

# Список организаций
curl http://localhost:8000/api/admin/organizations
```

## Данные из базы

На момент создания в базе данных:
- **1081** боец
- **55** событий
- **518** боев
- **14** организаций (уникальных из событий)

Все данные доступны для просмотра и редактирования через админ-панель.

